HINTS:
- The "business_id" field in the business collection corresponds to the "business_ref" fields in both the review table and the tip table. The values differ only by their prefixes: "business_id" uses the prefix businessid_, while "business_ref" uses the prefix businessref_. For example, businessid_1 in the business collection corresponds to businessref_1 in the review and tip tables.
- To join MongoDB business data with DuckDB review data, ALWAYS use this exact pattern in execute_python:
  df_business['id'] = df_business['business_id'].str.replace('businessid_', '').astype(int)
  df_reviews['id'] = df_reviews['business_ref'].str.replace('businessref_', '').astype(int)
  merged = pd.merge(df_reviews, df_business, on='id')
- The datasets contain five tables/collections in total. Carefully identify which tables or collections and which fields contain the information required to answer the query.
- The business collection's "description" field includes location information. To extract US state, use: df['state'] = df['description'].str.extract(r',\s*([A-Z]{2})\s*\d{5}')
- The business collection's "attributes" field includes services information (WiFi, parking, credit cards etc.) stored as a nested dict.
- When processing tool results in execute_python, ALWAYS wrap results in pd.DataFrame() before accessing fields. Never use integer indices on dict results.
- When a tool result is stored as a file path (string ending in .json), read it with: import json; data = json.load(open(file_path))
- Always compute average ratings from the DuckDB review table's rating field, not from MongoDB review_count metadata.
- When returning a state name and number together, format as: "PA: 3.70" to keep them close together in the answer.
